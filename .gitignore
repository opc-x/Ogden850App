node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
.vercel
.env*.local

# IDE / agent local
.cursor/
.claude/
.agents/

# Playwright / MCP artifacts
.playwright-mcp/
.playwright-screenshots/
test-results/

# Design pilot outputs (audit JSON, not source)
Designs/

# Vite PWA dev service worker (regenerated on dev)
dev-dist/

# 场景对话句 MP3（edge-tts 预生成，须随部署提交；npm run audio:scenes）
# public/audio/  — 勿忽略，否则 Vercel 线上无声

# 850 词 MP3 不提交（生产走 omega CDN fallback）
public/assets/audio/*
!public/assets/audio/guides/
!public/assets/audio/guides/**

# 废弃格式：插画只保留 PNG，场景只保留 WebP
public/assets/word-img/*.svg
public/assets/word-img/*.jpg
public/assets/scenes/*.svg
public/assets/scenes/*.png

# Pipeline backups & audit reports
*.bak-batch
*.report.json
*.bak
*.tmp
*.swp
*~

# Design / agent scratch at repo root (app assets live under public/)
/*.png
/*.jpg
/*.jpeg
/*.gif
/*.webp
/*.svg
/*.mp3
/*.mp4

# Cursor agent canvas scratch (chat artifacts, not source)
canvases/

# One-off LLM scene repair scratch (truth source: src/data/scenes/*.json)
scripts/repair-scenes-gemini.ts
scripts/apply-expert-fixes.ts
scripts/fix-dialogue-quality.ts
scripts/diagnose-failures.ts
scripts/generate-dialogues-openai.ts
